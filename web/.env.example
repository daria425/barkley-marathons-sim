VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_WS_URL=ws://127.0.0.1:8000/ws
VITE_MAPTILER_KEY=
# true = skip the WebSocket and prefill the store from src/mocks/mockRaceState.json once,
# for iterating on UI (panels, monologue feed) without a backend running.
VITE_USE_MOCK_DATA=false
